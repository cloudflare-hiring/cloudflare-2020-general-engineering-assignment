{
  "private": true,
  "name": "linktree-web",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "spark2@macalester.edu <spark2@macalester.edu>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
